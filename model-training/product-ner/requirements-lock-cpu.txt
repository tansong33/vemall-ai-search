# Exact combination verified end-to-end (train -> evaluate -> export -> pytest) on a
# CPU-only Linux box while producing this repo. Use it if the pins above give you
# trouble; it is older but proven.
torch==2.3.1
transformers==4.57.6
tokenizers==0.22.2
huggingface-hub==0.36.2
safetensors>=0.4
numpy>=1.26
PyYAML>=6
pytest>=8
