# Exact combination verified end-to-end (train -> evaluate -> serve -> pytest) on a
# CPU-only Linux box while producing this repo. Use it if the pins above give you
# trouble; it is older but proven.
torch==2.3.1
transformers==4.57.6
tokenizers==0.22.2
huggingface-hub==0.36.2
safetensors>=0.4
numpy>=1.26
PyYAML>=6
fastapi>=0.115
uvicorn>=0.30
pydantic>=2.7
pytest>=8
httpx>=0.27
