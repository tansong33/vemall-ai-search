# 数据目录约定

- `examples/`：可提交的脱敏小样例；
- `raw/`：原始脱敏 Query，本地使用，不提交；
- `gold/`：完成仲裁的版本化数据，不提交；
- `silver/`：单人复核或高一致性弱标，不提交；
- `splits/`：按 group 切分的 train/dev/test，不提交。

建议在公司数据目录保存一份 manifest：数据版本、生成时间、上游表/日志时间范围、脱敏规则版本、标签手册版本、样本数、SHA-256 和负责人。
